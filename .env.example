# Local secrets and proxy configuration. Copy to .env; .env is ignored.

# Postgres database with VectorChord Suite (`vchord`, `vchord_bm25`,
# `pg_tokenizer`, and `vector`) installed. Required for catalog, jobs,
# lexical search, semantic search, and usage records in every runtime mode.
YUTOME_POSTGRES_URL=postgresql://yutome:yutome@localhost:5432/yutome

# Voyage embeddings. Needed for semantic/hybrid search.
VOYAGE_API_KEY=

# Generic HTTP/SOCKS proxy used by youtube-transcript-api and yt-dlp.
# Examples:
# YUTOME_PROXY_URLS=http://user:pass@host1:port,socks5://user:pass@host2:port
# YUTOME_HTTP_PROXY=http://user:pass@host:port
# YUTOME_HTTPS_PROXY=http://user:pass@host:port

# Webshare rotating residential proxy config for youtube-transcript-api and yt-dlp.
# YUTOME_WEBSHARE_USERNAME=
# YUTOME_WEBSHARE_PASSWORD=
# YUTOME_WEBSHARE_DOMAIN=p.webshare.io
# YUTOME_WEBSHARE_PORT=80

# YouTube subscription imports. Keep the OAuth JSON file outside git.
# YUTOME_YOUTUBE_OAUTH_CLIENT_SECRETS=/absolute/path/to/client_secret.json
# YUTOME_YOUTUBE_API_KEY=

# Gemini fallback. GEMINI_API_KEY or GOOGLE_API_KEY is read by the google-genai client.
# GEMINI_API_KEY=
# GOOGLE_API_KEY=

# Remote API/MCP access. Generate with: yutome remote prepare
# YUTOME_HTTP_TOKEN=
# YUTOME_HTTP_CORS_ORIGINS=https://client.example
